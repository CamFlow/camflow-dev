config SECURITY_PROVENANCE
         bool "Provenance"
         depends on RELAY
         depends on SECURITY
         select SECURITYFS
         default n
         help
          This selects Provenance.
          Provenance is an helper to capture provenance data.
